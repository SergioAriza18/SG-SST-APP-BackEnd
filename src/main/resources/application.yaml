spring:
  application:
    name: sgsst-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/sgsst
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true